@inject NavigationManager NavigationManager

<button @onclick="CreateGame" class="btn btn-primary">New User Stats</button>

@code {
    void CreateGame()
    {
        NavigationManager.NavigateTo("/edit-game");
    }
}